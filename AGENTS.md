# AI Agent Instructions

## Current workspace state
- The repository is empty at present.
- No source code, documentation, build files, or project metadata are available.

## Guidance for AI agents
- Do not assume any language, framework, or build system until project files appear.
- If asked to perform development work, first confirm the intended project type and add code only when the user explicitly wants a new project initialized.
- When files are later added, inspect root-level manifests and docs such as `README.md`, `package.json`, `pyproject.toml`, `go.mod`, `Cargo.toml`, `*.sln`, or `*.csproj` to infer the stack.

## Important note
- This file exists to help agents understand the current empty state and avoid making unsupported assumptions.
